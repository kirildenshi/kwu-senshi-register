{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "node_modules/.bin/prisma migrate deploy; echo MIGRATE_EXIT=$?; echo ABOUT_TO_START_NEXT; node_modules/.bin/next start; echo NEXT_EXIT=$?; sleep 30",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
